"""
    容器通用操作：
        01）max(容器)：获取容器中最大的元素；
        02）min(容器)：获取容器中最小的元素；
        03）len(容器)：获取容器中元素的个数；
        04）list(容器)：将绒球转为列表；
        05）tuple(容器)：将绒球转为元组；
        06）str(容器)：将绒球转为字符串；
        07）set(容器)：将绒球转为集合；
        08）sorted(容器[,reverse = True])：容器排序，reverse = True 时表示降序；
"""